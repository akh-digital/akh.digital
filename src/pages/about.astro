---
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import AboutTitle from "../components/about/Title.astro";
import AboutSkills from "../components/about/Skills.astro";
import AboutSocials from "../components/about/Socials.astro";
import Resumes from "../components/about/Resumes.astro";
import GoToTop from "../components/GoToTop.astro";
import { global, localeFromPath } from "../lib/config";

const locale = localeFromPath(Astro.url.pathname);
---

<BaseLayout>
	<slot slot="head">
		<title>{global(locale).title} | {locale === "ru" ? "Обо мне" : "About"}</title>
	</slot>
	<main class="main column">
		<Header />
		<AboutTitle />
		<AboutSkills />
		<AboutSocials />
		<Resumes />
		<GoToTop />
	</main>
	<Footer />
</BaseLayout>

<style>
	:root {
		--body-color: var(--primary);
		--logo-color: var(--white);
	}
</style>
