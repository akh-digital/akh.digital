<div class="post">
	<slot />
</div>

<style>
	.post {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 24px;
		align-items: start;

		@media (max-width: 600px) {
			grid-template-columns: 1fr;
			gap: 24px;
		}

		> p {
			margin: 0;
		}

		> p:not(:has(img)) {
			grid-column: span 1;
		}

		> p:has(img) {
			grid-column: 1 / -1;
			display: block;
			width: 100%;
			overflow: clip;
			border-radius: 0px;

			@media (width < 1024px) {
				border-radius: 0px;
			}

			img {
				object-fit: contain;
				width: 100%;
				height: 100%;
			}
		}

		a {
			color: var(--primary);
		}
	}
</style>
