<div class="post">
	<slot />
</div>

<style>
	.post {
		display: grid;
		gap: 40px;

		@media (max-width: 600px) {
			gap: 40px;
		}

		p {
			margin: 0;
		}

		p:not(:has(img)) {
			word-break: normal;
			display: grid;
			grid-template-columns: 1fr;
			row-gap: 24px;
			column-gap: 24px;
			text-wrap: balance;
			margin-top: 60px;
			text-align: left;
		}

		p:has(img) {
			display: block;
			width: 100%;
			overflow: clip;
			border-radius: 0px;

			@media (width < 1024px) {
				border-radius: 0px;
			}

			img {
				object-fit: contain;
				width: 100%;
				height: 100%;
			}
		}

		a {
			color: var(--primary);
		}
	}
</style>
