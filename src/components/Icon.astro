---
import { Icon as AstroIcon } from "astro-icon/components";

const { name, class: className = "", size = 60, ...attrs } = Astro.props;
---

<AstroIcon
	name={name}
	size={size}
	class:list={["icon", "color: currentColor", className]}
	style="overflow: visible;"
	{...attrs}
/>
