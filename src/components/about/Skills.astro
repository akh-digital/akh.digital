---
import { about } from "../../lib/config.js";

const skills = about().skills;
---

<section class="skills" data-reveal>
	<div class="wrapper">
		{
			skills.map((skill) => (
				<div class="item" data-reveal>
					<div class="item-name" data-reveal-text>{skill.title}</div>
					<ul class="list">
						{skill.list.map((item) => (
							<li class="list-item" data-reveal-text>{item}</li>
						))}
					</ul>
				</div>
			))
		}
	</div>
</section>

<style>
	.skills {
		margin-top: 40px;
	}

	.wrapper {
		display: grid;
		grid-template-columns: repeat(2, 1fr);

		@media (width < 1024px) {
			grid-template-columns: repeat(1, 1fr);
		}
	}

	.item-name {
		font-weight: 400;
		font-size: var(--font-size-h2);
	}
	.list-item {
		line-height: 1.15;
		font-size: var(--font-size-normal);
	}
</style>
