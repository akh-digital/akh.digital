---
import Links from "./components/Links.astro";
import LinksLink from "./components/LinksLink.astro";
import { about, localeFromPath } from "../../lib/config.js";

const socials = about(localeFromPath(Astro.url.pathname)).social;
const list = socials.list;
---

<Links>
	<Fragment slot="header">
		{socials.title}
	</Fragment>
	<Fragment slot="second-title">
		{socials.second_title}
	</Fragment>
	<Fragment slot="links-list">
		{
			list.map((social) => (
				<LinksLink url={social.url} title={social.title} />
			))
		}
	</Fragment>
</Links>
